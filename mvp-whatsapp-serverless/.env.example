# BrasilGuard / Mordomo — MVP WhatsApp Serverless
# TEMPLATE ONLY. Nunca colocar segredo real neste arquivo versionado.
# Padrão BGD para qualquer senha/token/API key/secret que exija troca manual: mudeaquiasenha

# Meta / WhatsApp — SEGREDOS
WHATSAPP_VERIFY_TOKEN=mudeaquiasenha
WHATSAPP_APP_SECRET=mudeaquiasenha
WHATSAPP_ACCESS_TOKEN=mudeaquiasenha

# Meta / WhatsApp — NÃO SECRETOS
WHATSAPP_PHONE_NUMBER_ID=SEU_PHONE_NUMBER_ID
WHATSAPP_GRAPH_VERSION=vXX.X

# Google Gemini
GEMINI_API_KEY=mudeaquiasenha
GEMINI_MODEL=gemini-3.5-flash

# BrasilGuard internal service-to-service secret
BGD_INGRESS_SECRET=mudeaquiasenha

# Cloudflare -> Supabase — endpoint não secreto
SUPABASE_INGEST_URL=https://hnpwcffosfvpypglcscy.supabase.co/functions/v1/wa-ingest

# Gate BGD: produção deve ser bloqueada enquanto qualquer segredo ativo for "mudeaquiasenha".
